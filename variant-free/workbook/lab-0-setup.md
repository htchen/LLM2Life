# Lab 0：建立你自己的教材資料夾（25 分鐘）

## 這一段結束時，你會有

- 一個 claude.ai 帳號（**免費**）
- 一個 GitHub repo，而且是 **Public**
- 學會把 AI 回覆建立成檔案

> 今天不需要安裝軟體或付費方案。學員需要手動建立與更新檔案，
> 這份手冊會逐步說明；後續 Lab 會重複相同流程。

---

## 步驟 1：開一個 claude.ai 帳號

打開 https://claude.ai ，用 Email 或 Google 帳號註冊。**免費方案就夠了，不用訂閱。**

> ⚠️ **登入後畫面上方可能會看到一個 Code 分頁——今天請不要點它。**
> 那是需要付費的功能。我們今天只用**一般的聊天視窗**（畫面中間可以打字的那個框），
> 跟你平常跟 AI 聊天一樣。

**你應該會看到**：一個可以打字問問題的聊天畫面。

---

## 步驟 2：開一個 GitHub 帳號

打開 https://github.com/signup 註冊，免費方案就夠了。

註冊完請**收信點開驗證信**完成驗證——沒驗證的帳號沒辦法建立儲存庫，
如果沒有收到驗證信，請檢查垃圾郵件匣。

**把你的帳號名稱記起來**，等一下要用。

---

## 步驟 3：建立你的 repo

打開 https://github.com/new，填三個地方：

| 欄位 | 填什麼 |
| --- | --- |
| Repository name | `my-life-tools` |
| Public / Private | **Public** ⚠️ |
| Add a README file | **打勾** ✅ |

按最下面的 **Create repository**。

> ⚠️ **一定要選 Public。** 今天最後要把作品變成公開網址，
> 免費帳號的 Private repo 做不到。

**你應該會看到**：一個網址是 `https://github.com/你的帳號/my-life-tools` 的頁面，
裡面有一個 `README.md`。

---

## 步驟 4：把文字建立成檔案

Claude 的聊天回覆是文字，不會自行成為可開啟的網頁檔案。
今天每一個 Lab，你都會重複同一個動作四步驟：

```
複製 Claude 給的文字  →  貼到純文字編輯器  →  存成正確的檔名  →  雙擊打開
```

現在先練習一次，後續 Lab 會直接使用相同操作。

### Windows：用「記事本」

1. 點左下角開始選單，打字搜尋「**記事本**」，打開它
2. 複製貼上這段測試文字：

```html
<h1>Hello, 我做出來了！</h1>
```

3. 上方選單 **檔案 → 另存新檔**
4. **「存檔類型」選「所有檔案 (\*.\*)」**，避免自動加上 `.txt`
5. 「檔案名稱」直接打 `test.html`（要包含 `.html`）
6. 存到桌面，按存檔
7. 到桌面找到那個檔案，**滑鼠雙擊**

**你應該會看到**：瀏覽器打開，畫面上一行大字「Hello, 我做出來了！」

> ⚠️ **如果雙擊後開的是記事本，不是瀏覽器**——代表你的檔案其實叫
> `test.html.txt`。回到第 4 步，「存檔類型」一定要選「所有檔案」，不能留在
> 「文字文件 (\*.txt)」。

### Mac：用「文字編輯」

1. 按 `Cmd + Space` 打開 Spotlight，打字搜尋「**文字編輯**」，打開它（TextEdit）
2. 上方選單選 **格式 → 製作純文字**，避免儲存成 RTF
   （快速鍵 `Shift + Cmd + T`）
3. 複製貼上這段測試文字：

```html
<h1>Hello, 我做出來了！</h1>
```

4. 上方選單 **檔案 → 儲存**
5. 檔案名稱打 `test.html`（要包含 `.html`）
6. 存到桌面，按儲存
7. 到桌面找到那個檔案，**滑鼠雙擊**

**你應該會看到**：瀏覽器打開，畫面上一行大字「Hello, 我做出來了！」

> ⚠️ **如果開啟後顯示程式碼或格式不正確**，請確認已完成第 2 步「製作純文字」。
> 文字編輯預設會儲存成 RTF，瀏覽器不會把它當成 HTML。
> 重新開一個新檔案，記得先按 `Shift + Cmd + T`，再貼文字。

---

## 步驟 5：把 GitHub Pages 開起來

**現在先開好，最後要發布時才不用等。**

1. 在你的 repo 上方按 **Settings**
2. 左邊選單往下捲，找到 **Pages**（在 "Code and automation" 那一區，**不是上排的分頁**）
3. **Source** 選 **Deploy from a branch**
4. **Branch** 選 **main**，右邊資料夾保持 `/ (root)`，按 **Save**

**你應該會看到**：頁面上方出現一段訊息，說網站正在建置（Your site is live 或
正在部署中）。

> **第一次建置要等 1–10 分鐘，這是正常的，現在不用等它。**
> 重點是先開起來——等你做完專題要發布時，它早就準備好了，
> 上傳完幾十秒就看得到網址。如果留到最後才開，你會在最需要的時候等最久。

---

## ✅ Lab 0 檢查點

- [ ] 我有一個 claude.ai 帳號，登入後看得到聊天畫面
- [ ] 我的 GitHub repo `my-life-tools` 已經建好，而且是 **Public**
- [ ] 我剛剛雙擊 `test.html`，瀏覽器打開並顯示「Hello, 我做出來了！」
- [ ] **Settings → Pages** 已設為 **Deploy from a branch**／**main**

四項都完成後，即已具備課程需要的工具與操作方式。

**沒有看到「Hello」，只看到程式碼或亂碼？** 請舉手，後續每個 Lab 都會使用相同的存檔方式。

---

## 需要追上進度？

如果測試檔案怎麼存都不對，換個角度：**先確認你的電腦有沒有「顯示副檔名」**。

- **Windows**：檔案總管 → 上方「檢視」→ 打勾「副檔名」，這樣你才看得到
  自己存的檔案到底叫 `test.html` 還是 `test.html.txt`
- **Mac**：文字編輯的存檔視窗裡，通常有一個「若沒有副檔名則使用 .txt」的選項，
  **確認它沒有被勾選**

如果仍無法完成，請舉手找講師協助檢查存檔設定。
